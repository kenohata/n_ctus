<%= form_for([@note_image.note, @note_image]) do |f| %>
  <% if @note_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@note_image.errors.count, "error") %> prohibited this note_image from being saved:</h2>

      <ul>
      <% @note_image.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :image %><br />
    <%= f.file_field :image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
