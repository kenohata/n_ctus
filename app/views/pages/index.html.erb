<h1>Listing pages</h1>

<table>
  <tr>
    <th>Note</th>
    <th>User</th>
    <th>Content</th>
    <th>Attachment</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @pages.each do |page| %>
  <tr>
    <td><%= page.note_id %></td>
    <td><%= page.user_id %></td>
    <td><%= page.content %></td>
    <td><%= page.attachment %></td>
    <td><%= link_to 'Show', [page.note, page] %></td>
    <td><%= link_to 'Edit', edit_note_page_path(page.note, page) %></td>
    <td><%= link_to 'Destroy', [page.note, page], confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Page', new_note_page_path %>
